#ifndef GROUNDWINDOW_H
#define GROUNDWINDOW_H

#include <QWidget>

class GroundWindow : public QWidget
{
    Q_OBJECT
public:
    explicit GroundWindow(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // GROUNDWINDOW_H